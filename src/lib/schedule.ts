export function schedule(data: any[]) {

    data.forEach(person => {
        let schedule = [];
        const choices = [person.Choice1, person.Choice2, person.Choice3, person.Choice4, person.Choice5, person.Choice6, person.Choice7];   
        let student = {
            "Block1": "",
            "Block2": "",
            "Block3": "",
            "Block4": ""
        };
        choices.forEach(choice => {
            // TODO
        });
    });
}